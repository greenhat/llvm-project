static_library("Utils") {
  output_name = "LLVMMidenUtils"
  deps = [
    "//llvm/lib/CodeGen",
    "//llvm/lib/IR",
    "//llvm/lib/MC",
    "//llvm/lib/Support",

    # MCTargetDesc depends on Utils, so we can't depend on the full
    # MCTargetDesc target here: it would form a cycle.
    "//llvm/lib/Target/Miden/MCTargetDesc:tablegen",
    "//llvm/lib/Target/Miden/TargetInfo",
  ]
  include_dirs = [ ".." ]
  sources = [
    "MidenTypeUtilities.cpp",
    "MidenUtilities.cpp",
  ]
}
